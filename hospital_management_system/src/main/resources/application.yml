spring:
  datasource:

  # POSTGRESQL Configuration
  driver-class-name: org.postgresql.Driver
  password: postgres
  username: postgres
    url: jdbc:postgresql://localhost:5432/hospital_management_system

  # MYSQL Configuration
    # url: ${DB_URL:jdbc:mysql://localhost:3306/hospital_management_system?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    # username: ${DB_USERNAME:root}
    # password: ${DB_PASSWORD:123456}
    # driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

  application:
    security:
      jwt:
        # Dev defaults so the app can start locally without env vars.
        # Override in production via environment variables.
        secret-key: ${JWT_SECRET:change-me-change-me-change-me-change-me}
        expiration: ${JWT_EXPIRATION:86400000}